// Re-export from new location for backwards compatibility
export type { CharacterState, Direction, TileKind, DecorationKind, GridTile, Decoration, Character, WorldState, SelectedObject, SceneConfig } from './core/sceneTypes'
