export enum AlertLevel {
  INFO     = 'info',
  WARNING  = 'warning',
  ERROR    = 'error',
  CRITICAL = 'critical',
}

export enum AlertSource {
  OPENCLAW = 'openclaw',
  HOST     = 'host',
  IOT      = 'iot',
  MANUAL   = 'manual',
}
